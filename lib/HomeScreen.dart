import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget{
  static const String routename = 'home-sceen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}